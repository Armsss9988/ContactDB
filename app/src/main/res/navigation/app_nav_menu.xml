<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/app_nav_menu"
    app:startDestination="@id/contact">

    <fragment
        android:id="@+id/contact"
        android:name="com.example.contactdb.ContactDetails.ContactDetails"
        android:label="ContactDetails"
        tools:layout="@layout/fragment_contact_details"/>
    <fragment
        android:id="@+id/new_contact"
        android:name="com.example.contactdb.ContactForm.ContactFormFragment"
        android:label="ContactFormFragment"
        tools:layout="@layout/fragment_contact_form"/>
</navigation>